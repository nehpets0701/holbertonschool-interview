#include "regex.h"

/**
 * regex_match-checks if regex matches string
 * @str:string input
 * @pattern:regex input
 * Return: 0/1
 */
int regex_match(const char *str, const char *pattern)
{
	return (str && pattern);
}
